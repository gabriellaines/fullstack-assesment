# Fullstack Assessment

A fullstack app with Angular and Nestjs to maintain employees.

## Setup

1. **Set Up Docker and Start the Project:**
   Run the following commands to set up the environment and start the project:
   ```bash
   make start
   ```
   It will setup the .env variables, run docker-compose to create the database structure, 
   install both client and server dependencies and then start the project.

