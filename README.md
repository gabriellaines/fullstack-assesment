# Fullstack Assessment

A fullstack app with Angular and Nestjs to maintain employees.

## Setup

1. **Create the `.env` file:**
   The `.env` file will be automatically created with default values when you run `make setup`.

2. **Set Up Docker and Start the Project:**
   Run the following commands to set up the environment and start the project:
   ```bash
   make setup
   make all

